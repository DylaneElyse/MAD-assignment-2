import { View, Text, TextInput, Button, StyleSheet } from "react-native";
import React, {useState} from "react";
import { useRouter } from "expo-router";

export default function calgaryPage() {
    return(
    <View>
        <Text>This is the Calgary page</Text>
    </View>
    )
}